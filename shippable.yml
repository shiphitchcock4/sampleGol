

# Language setting
language: go

# Version number
go:
  #- 1.1 
  - 1.2
  - 1.3 
  
services:
  - mysql
  - postgres
  - mongodb  
  - elasticsearch
  - redis
  - memcache   
  - rabbitmq
matrix:
  include:
    - go: 1.1
  exclude: 
    - go: 1.2
  allow_failures:
    - go: 1.3 
  
build:
  pre_ci_boot:
    image_name: drydock/u12golpls
    image_tag: prod
    pull: true
    
  ci:
    - go version
    - if [ "$SHIPPABLE_GO_VERSION" == "1.3" ]; then foobar; fi

      
